# CLAUDE.md — (Flint) GITPULSE

**Start at `Mesh/(System) Flint Init.md`.** It is the contract for this workspace.

This vault documents the repository at `/Users/brunojaamaa/Desktop/gitpulse` — a TypeScript
CLI **published on npm as `@aethereumdev/gitpulse@1.0.0`**.

> ⚠️ **The branch is `master`, not `main`.** Any automation that assumes `main` will fail
> here. This is the only one of Bruno's three portfolio repos that does this.

> ⚠️ The Flint is registered as **`GITPULSE`** because `flint init` rejects `GitPulse`
> (mixed case). See [[(Report) Build Log]].

**The repo is the truth for code. This vault is the truth for state.** Where they disagree,
the repo wins and the note gets fixed.

Up one level: [[(Guide) BRUNO HQ]] → `/Users/brunojaamaa/Desktop/Main Vault/Main/Mesh/(Map) BRUNO HQ.md`.

Never run destructive git operations here. **Never run `npm publish`** — this package is
live on the public registry.
